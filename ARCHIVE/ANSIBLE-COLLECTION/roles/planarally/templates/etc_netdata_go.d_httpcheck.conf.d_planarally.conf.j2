  - name: planarally
    timeout: 1
    tls_skip_verify: true
    update_every: 10
    url: https://127.0.0.1
    headers:
      Host: "{{ planarally_fqdn }}"
{% if not planarally_enable_service %}
    status_accepted: [503]
{% endif %}
